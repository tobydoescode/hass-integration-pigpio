[project]
name = "ha-pigpio-integration"
version = "0.1.0"
description = "Home Assistant custom integration for remote Raspberry Pi GPIO via pigpio"
requires-python = ">=3.13"
dependencies = [
    "pigpio>=1.78",
]

[project.optional-dependencies]
dev = [
    "pyright>=1.1",
    "pytest>=8.0",
    "pytest-cov>=6.0",
    "pytest-homeassistant-custom-component>=0.13",
    "pre-commit>=4.0",
    "ruff>=0.11",
]

[tool.ruff]
# target-version is inferred from project.requires-python -- do not set it here,
# or the two drift and only one of them gets bumped.
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "C901", "PIE"]

[tool.coverage.run]
relative_files = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
